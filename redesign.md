
Keep using tanstack router.
jump to solid?
- need to replace dnd kit with framework independent one

drop jotai & repalce with tanstack store or solid.

indexedb?
need to figure out if worker is working on ios..
maybe a debug build showing everything happening.
